import React from "react";
import styles from "./Footer.module.scss";

function Footer() {
    return (
        <div>
            Display all the technologies i used,HTML, SCSS, React, Firebase
        </div>
    );
}

export default Footer;
